{
  "name": "teeline-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "deploy": "npm run build && wrangler pages deploy dist/"
  },
  "dependencies": {
    "@picocss/pico": "2.1.1",
    "htmx.org": "2.0.10"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vite": "8.0.14",
    "vitest": "4.1.7",
    "wrangler": "4.95.0"
  }
}
